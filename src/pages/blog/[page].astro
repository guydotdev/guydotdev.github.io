---
import Blog from './index.astro'

import { generateBlogPageRoutes } from '../../common/blog'

export async function getStaticPaths() {
  return await generateBlogPageRoutes()
}

const { page } = Astro.params
---

<Blog page={page} />
